import { makeStyles } from "@material-ui/core/styles";

export const useTreeViewStyles = makeStyles({
  root: {
    minHeight: 264,
    flexGrow: 1,
    maxWidth: 400
  },
  treeButton: {
    textAlign: "left",
    marginTop: 1,
    marginBottom: 1
  }
});
